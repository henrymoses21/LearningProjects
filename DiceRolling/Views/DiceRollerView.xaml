<UserControl x:Class="DiceRolling.Views.DiceRollerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiceRolling.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <local:DieFaceView Padding="4px" Grid.Column="0" IsNumeric="True" /> <!-- bind IsNumeric to checkbox output via viewmodel -->
        <local:DieFaceView Padding="4px" Grid.Column="1" />
        <local:DieFaceView Padding="4px" Grid.Column="2" />
        <local:DieFaceView Padding="4px" Grid.Column="3" />

        <!-- put roll button and numeric/dots checkbox here -->
        <!-- hook up roll to roll ALL dice -->
        <!-- get rid of exit button -->
    </Grid>
</UserControl>
